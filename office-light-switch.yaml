substitutions:
  device_name: office-light-switch
  friendly_name: Bouton lumiere bureau
  
  ## TX Ultimate ##
  relay_count: "3"
  switch_id_for_button_1: "relay_2"
  switch_id_for_button_2: "relay_1"
  switch_id_for_button_3: "relay_3"

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name} 

esp32:
  board: esp32dev
  #framework:
  #  type: arduino

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  use_address: ${device_name}.intra.sberard.fr

  ap:
    ssid: ${device_name}
    password: !secret ap_password

api:
  reboot_timeout: 15min
  encryption:
    key: !secret api_encryption

ota:
  password: !secret ota_password

captive_portal:

logger:
  level: DEBUG

web_server:

#######################################
# Device specific Config Begins Below #
#######################################
packages:
  device_base: !include packages/device_base.yaml
    #url: https://github.com/AntorFr/esphome-projects
    #file: packages/device_base.yaml
  tx_ultimate: !include packages/tx_ultimate.yaml